// dashboard.js
// Upload logic lives entirely in gallery-upload.js.
// Add any non-upload dashboard behaviour here (stats, nav, etc.)

document.addEventListener("DOMContentLoaded", () => {
  console.log("Dashboard ready")
})